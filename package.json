{
  "name": "lakonkcreative-bzh",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "prebuild": "node scripts/download-fonts.mjs || echo fonts-skip ; node scripts/fetch-google-images.mjs || echo gi-skip ; node scripts/fetch-wiki-images.mjs || echo wiki-skip ; node scripts/fetch-article-images.mjs || echo ai-skip",
    "build": "astro build && (pagefind --site dist || echo 'pagefind skipped — non-blocking')",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.2.0",
    "@tailwindcss/vite": "^4.1.0",
    "astro": "^5.0.0",
    "gray-matter": "^4.0.3",
    "pagefind": "^1.1.1",
    "tailwindcss": "^4.1.0"
  }
}
